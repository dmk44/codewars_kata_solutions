8 kyu
# Bin to Decimal
**DESCRIPTION**: Complete the function which converts a binary number (given as a string) to a decimal number.

BinaryFundamentals
## Solution
```py
bin_to_decimal = lambda x: int(x, base=2)
```